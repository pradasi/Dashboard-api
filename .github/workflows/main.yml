name: Reject non release PR
on:
  pull_request:
    branches: master

jobs:
  CheckIfMatching:
    runs-on: ubuntu-latest
    if: ${{ !endsWith(github.head_ref, 'Release') }}
    steps:
      - name: Pull number
        run: echo "${{ github.event.number }}"
      - name: Source branch
        run: echo "${{ github.head_ref }}"
      - name: conditon for brach
        run: | 
          curl -L --X PATCH -H "Accept: application/vnd.github+json" -H "Authorization: Bearer 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" -H "X-GitHub-Api-Version: 2022-11-28" https://api.github.com/repos/pradasi/Dashboard-api/pulls/${{ github.event.number }} -d '{"state":"close"}'
